import 'package:flutter/material.dart';

class RegularHomeScreen extends StatelessWidget {
  const RegularHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Regular Home Screen');
  }
}

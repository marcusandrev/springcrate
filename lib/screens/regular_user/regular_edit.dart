import 'package:flutter/material.dart';

class RegularEditScreen extends StatelessWidget {
  const RegularEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Regular Edit Screen');
  }
}